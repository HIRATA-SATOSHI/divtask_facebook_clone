<p id="notice"><%= notice %></p>

<h1>Pictures</h1>
<br>
<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Content</th>
      <th>Post</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td><%= image_tag picture.image.url %></td>
        <td><%= picture.content %></td>
        <td><%= picture.user.name %></td>
        <td><button type="button" class="btn btn-secondary"><%= link_to 'Show', picture %></button></td>
        <td><button type="button" class="btn btn-secondary"><%= link_to 'Edit', edit_picture_path(picture) %></button></td>
        <td><button type="button" class="btn btn-outline-danger"><%= link_to 'Destroy', picture, method: :delete, data: { confirm: 'Are you sure?' } %></button></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<button type="button" class="btn btn-info"><%= link_to 'New Picture', new_picture_path %></button>
